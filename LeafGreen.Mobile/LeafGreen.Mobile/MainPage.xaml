<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:LeafGreen.Mobile.ViewModels;assembly=LeafGreen.Mobile.Android"
             x:Class="LeafGreen.Mobile.MainPage">

    <ScrollView x:Name="GardensScrollView" IsVisible="False">
        <ListView ItemsSource="{Binding Gardens}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding GardenName}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.BindingContext>
                <viewModels:MainPageViewModel/>
            </ListView.BindingContext>
        </ListView>
    </ScrollView>
    <StackLayout x:Name="NoGardensStackLayout" 
                 IsVisible="False" 
                 VerticalOptions="Center" 
                 HorizontalOptions="Center">
        <Label Text="I don't see any gardens!" 
            FontSize="20" 
            TextColor="DimGray" />
        <Button Text="Tap here to add one!"/>
    </StackLayout>

</ContentPage>